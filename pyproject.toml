[project]
name = "sledding-saves"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "capstone>=5.0.7",
    "cryptography>=48.0.0",
    "unitypy>=1.25.0",
]
